#ifndef LINEVERSE_MAIN_H
#define LINEVERSE_MAIN_H

/**
 * @brief Application entry wrapper for the standalone IdiomChainGame test package.
 */
class LineVerseApp {
public:
    /**
     * @brief Run the application.
     * @return 0 on normal exit, non-zero on failure.
     */
    int run();
};

#endif
